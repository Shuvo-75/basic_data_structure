#include<bits/stdc++.h>
using namespace std;
int main()
{
    int s;
    cin >> s;
    string str[s];
    for(int i = 1; i <= s; i++)
    {
        cin >> str[i];
    }

    // red + blue = purple
    // red + green = yellow
    // blue + green = cyan

    

    return 0;
}